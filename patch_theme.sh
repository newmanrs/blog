# Couldn't get the en.toml in template to work with same tools as the submodules, so I'm writing this patch script to copy a local file from main repo into the submodule.  This is a bit janky.

cp i18n/en.toml themes/hugo-future-imperfect-slim/i18n
